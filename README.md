# Labelling App For OCR

A Django-based web application for **labelling Nepali words from images**. The system provided an interface for reviewing and managing existing labels, with authentication and administrative functionality.

## ✨ Features

* 🔐 User login and authentication
* 🖼️ Display images containing Nepali words
* ✏️ Edit existing labels
* 🗑️ Delete labels
* ➕ Update and manage labels
* 🔎 Filter labelled data
* 👤 User management
* 🛠️ Admin interface for managing the labelling system

## 📂 Project Structure

```text
Labelling Server/
│
├── labelconf/          # Django project configuration
├── labelmanager/       # Main labelling application
├── manage.py            # Django management script
└── .gitignore
```

## 🛠️ Technologies

* Python
* Django
* HTML
* CSS
* JavaScript
* SQLite

## 🎯 Purpose

The application was built to support the creation and management of a **Nepali word image dataset** by allowing users to review images and create, update, filter, and delete their associated labels.

The system was designed as a practical tool for preparing labelled data for **Nepali language / OCR-related machine learning tasks**.

## 📌 Status

**Archived / Older Project**

This is an older project built while working with Django and exploring tools for managing labelled datasets.
